// OpenModelica script to check installation by simulating SimpleRC
loadFile("SimpleRC.mo");
// Time constant tau = R*C = 1000 * 0.001 = 1s, so simulate for 5*tau to see
// the full exponential charging curve (reaches ~99.3% of Vsource by t=5s).
simulate(SimpleRC, stopTime=5);
val(Vc, 5);
